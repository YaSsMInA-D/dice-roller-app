version: '3.8'

services:
  dice-app:
    build: .
    ports:
      - "${APP_PORT:-5000}:5000"
    environment:
      - FLASK_ENV=development
      - DICE_SIDES=${DICE_SIDES:-6}
    restart: unless-stopped